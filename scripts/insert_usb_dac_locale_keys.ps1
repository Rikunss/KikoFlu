# DEPRECATED: This script inserted `usbDacBypass` locale keys into l10n files.
# The `usbDacBypass` feature has been removed and replaced by `bitPerfectPlayback`.
# Locale keys are now managed via ARB files and `flutter gen-l10n`.
# See git history for the original script.
